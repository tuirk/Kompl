# Placeholder — real Dockerfile lands in build step 2.
# The Python FastAPI NLP service image will be built here once
# docker-compose.yml wires the build context. v1's Dockerfile is a
# starting reference (preloaded spaCy model, sentence-transformers cache).
FROM alpine:latest
