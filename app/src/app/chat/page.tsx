'use client';

import ChatPanel from '@/components/ChatPanel';

/** Legacy /chat route — full-page panel. Primary entry is the TopNav drawer. */
export default function ChatPage() {
  return <ChatPanel variant="page" />;
}
