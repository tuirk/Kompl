# Placeholder — real Dockerfile lands in build step 2.
# The Next.js app image will be built here once docker-compose.yml wires
# the build context. See docs/prd/ for the intended app surface.
FROM alpine:latest
