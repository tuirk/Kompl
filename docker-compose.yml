# Kompl v2 — placeholder docker-compose.
#
# This file is a SCAFFOLD only. It declares the four services by name with
# alpine:latest placeholders so `docker compose config` parses. Real images,
# build contexts, ports, volumes, env vars, and healthchecks land in build
# step 2 of the v2 plan, after the SQLite schema migration script exists.
#
# See:
#   - docs/prd/ for the product spec
#   - CLAUDE.md "Non-negotiable architecture rules (v2)" for the constraints
#   - C:\Users\tuana\.claude\plans\snoopy-moseying-eagle.md for the build order
#
# Architecture rule #1 (single-writer SQLite) means only the `app` service
# will mount /data/db with write access in step 2. Every other service talks
# to SQLite through HTTP calls to `app`.

services:
  app:
    image: alpine:latest

  nlp-service:
    image: alpine:latest

  n8n:
    image: alpine:latest

  wiki:
    image: alpine:latest
