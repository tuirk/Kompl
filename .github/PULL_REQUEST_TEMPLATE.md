<!--
Thanks for the PR. A few quick things:

- Sign off every commit with `git commit -s` (DCO check is required — PR can't merge without it).
- Smaller PRs review faster. If yours is >300 lines, consider opening an issue to discuss first.
- See CONTRIBUTING.md for full details.
-->

## What changed

<!-- One or two sentences. The diff already shows the *what* — explain the *why*. -->

## Why

<!-- What problem does this solve, or what does it enable? -->

## How was this tested?

<!-- Did you run the unit tests? The integration test? A manual flow? -->

- [ ] Unit tests pass locally (`npm test` in `app/` and `cli/`, `pytest` in `nlp-service/`)
- [ ] Manually exercised the change end-to-end
- [ ] N/A — explain why

## Anything else reviewers should know?

<!-- Edge cases, breaking changes, follow-ups, screenshots if it's a UI change. -->
